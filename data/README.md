# Structured Data

`drafts/v12-tranche-01.json` is the machine-readable form of the first V12
particularisation tranche. It validates against
`../schemas/servati-dataset.schema.json` and uses the relevant OnlyWorlds base,
Location, and Event vocabulary.

The data layer supports indexing, validation, maps, timelines, graphs, and
future exports. It does not supersede the human-readable DRAFT entries or
promote them to canon.

The detailed Version 11 seed supplied by the working pack is not in this
directory because its manuscript citations cannot yet be inspected. It is
preserved under `../research/v11-source-review/` with source-unavailable status.
